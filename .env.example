PUBLIC_SUPABASE_URL=https://<project-id>.supabase.co
PUBLIC_SUPABASE_ANON_KEY=sb_publishable_<key>

# Optional: RunOwl backend (Python FastAPI)
RUNOWL_BACKEND_URL=http://localhost:8000

# CI / demo mode — set to true to bypass Supabase and use seed data
USE_MOCK_DATA=false
